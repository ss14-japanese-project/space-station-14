Fonts are hardcoded into the game.

This folder exists to make it easier to revert the overwritten fonts once that stops being the case.

- - - - -

フォントはゲーム内にハードコードされています。

このフォルダは、上書きされたフォントを、ハードコードされたフォントが不要になった際に簡単に元に戻せるようにするために存在します。
